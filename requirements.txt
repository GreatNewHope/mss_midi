# Dependencies used directly by this repository's scripts.
#
# `make prepare` installs only the core separation runtime. Optional choir-part,
# polyphonic-MIDI, and GAME pipelines install their own requirements on demand.
huggingface-hub
melband-roformer-infer
PyYAML
torch
torchaudio
